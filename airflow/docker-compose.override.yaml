services:
  airflow-worker:
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    group_add:
      - "999" 
  airflow-apiserver:
    ports:
      - "8090:8080"

networks:
  default:
    name: waba-network
    external: true